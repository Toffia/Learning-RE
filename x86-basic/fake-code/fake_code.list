func (char* name, char* str)
{
	i = strlen(name);
	if (i<8)
		for (i; i<8; i++)
		{
			str[name] = 0x61+i;	
		}

	for (int i=0; j=0; i<16; i++)
	{	
		buff2[i] = str[j]+1;
		buff2[i] = 0x41 + i + first_length;
	}
}


int main()
{
	char name[40], str[40];

	memset(name, '0',40);
	memset(str, '0',40);

	printf("Insert name ");
	scanf("%s")

	if (strlen(name)>=16)
		printf("Sorry, your name is too long");
	else
	{
		func(name, str);	
		printf("Your license key is %s", str);
	}

	return 0;
}